contract AuditInterface
{
	struct AuditContext
	{
		uint64 app_cvid;
		address sender;
		string func_name;
		array<uint8> input;
	}

	interface IAuditCheck
	{
		@global function bool check(AuditContext ctx) public export;
	}
}
